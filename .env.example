PORT=3000
NODE_ENV=production          # use "production" on Railway
API_KEY=                     # generate with: openssl rand -hex 32
REDIS_URL=redis://localhost:6379  # on Railway use ${{Redis.REDIS_URL}}; empty disables queue/worker

# Readiness probe (/api/ready): max time (ms) for Redis PING + queue metrics (default 2000)
READY_CHECK_TIMEOUT_MS=2000

# --- Health endpoints (see README.md) ---
# GET /api/health  → liveness only (Railway healthcheckPath); no Redis
# GET /api/ready   → Redis + queue readiness; 503 if not ready

S3_ENDPOINT=                 # optional — leave empty if not using S3
S3_BUCKET=                   # optional — leave empty if not using S3
S3_ACCESS_KEY=               # optional — leave empty if not using S3
S3_SECRET_KEY=               # optional — leave empty if not using S3
DEFAULT_MAX_DEPTH=3
DEFAULT_MAX_PAGES=50
PAGE_TIMEOUT_MS=30000
JOB_TIMEOUT_MS=300000
REQUEST_DELAY_MS=1500
BROWSER_CONCURRENCY=3
